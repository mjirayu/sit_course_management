extends ../client-layout
block script
  
block content
  nav#main-nav.navbar(role='navigation')
    .container
      .navbar-header
        button.navbar-toggle(type='button', data-toggle='collapse', data-target='#main-manu')
          span.sr-only Toggle navigation
        a.navbar-brand(href='/') SIT
      #main-manu.collapse.navbar-collapse.navbar-right
        ul.nav.navbar-nav
          li.active
            a(href='#fakelink') Home
          li
            a(href='#fakelink') Forum
          li
            a(href='#fakelink') Contact
    // /navbar
    // Main contain
  .container(ng-controller="dndController")
    ul(ng-repeat='item in courselist')
      li test{{item}}
    .row
      .col-md-12
        h2.text-center.title
          span
            | {{models.name}}{{testname}}
    .row
      .col-md-8
        #dnd.block.radius
          .row(ng-repeat='row in models.course_list')
            .title
              | {{'Year ' + row.year + ' Semester ' + row.semester}}
            ul(dnd-list='row.course',dnd-allowed-types="34",dnd-disable-if="false")
              .dnd-row
                div.coursebox(ng-repeat='item in row.course', dnd-moved='row.course.splice($index, 1)', dnd-effect-allowed='move', dnd-disable-if='row.year < year.year || (row.year <= year.year && row.semester <= year.semester)', dnd-draggable='item', ng-class="{'disable': row.year < year.year || (row.year <= year.year && row.semester <= year.semester),'enable': 1==1,'elective':item.type == 2}")
                  li.radius(ng-click='courseFun()' )
                    span {{item.course_id +" "+item.name}}
                  li.radius(ng-show="item.type == 2")
                    a(type='button', data-toggle='modal', data-target='#myModal')
                      | ลงทะเบียน
      .col-md-4
        .block.radius
          h3.underline
            span Message
          p
            | พูดบางอย่างถึงอาจารย์เกี่ยวกับแผนการเรียนของคุณ
          form
            input(type="text",placeholder="ข้อความ")
            button.btn.btn-primary(type="submit") ส่ง
    .row
      .col-md-12
        .block.radius
          pre.code.
            {{modelAsJson}}   









  #myModal.modal.fade(tabindex='-1', role='dialog', aria-labelledby='myModalLabel')
    .modal-dialog(role='document')
      .modal-content
        .modal-header
          button.close(type='button', data-dismiss='modal', aria-label='Close')
            span(aria-hidden='true') ×
          h4#myModalLabel.modal-title ลงทะเบียน
        .modal-body
          ul(ng-repeat='item in courselist')
            li test{{item}}
            
        
          label.radio 
            input#inlineRadio1(type='radio', name='inlineRadioOptions', value='option1')
            |CSC101 JAVA 1  
            

        .modal-footer
          
          button.btn.btn-primary(type='button') Save changes
